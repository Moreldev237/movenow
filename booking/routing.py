from django.urls import re_path

websocket_urlpatterns = [
    # Example WebSocket URL patterns for booking
    # re_path(r'ws/booking/(?P<booking_id>\d+)/$', consumers.BookingConsumer.as_asgi()),
]

